## プロジェクト概要

<プロジェクトに合わせて修正する>

---

## 作業ルール

- `node_modules` `dist` 配下は原則参照しない。参照が必要な場合はユーザーに許可を得てから行う

---

## GitHub ワークフロー

- 必ず `feature-<概要>` ブランチを作成し PR を発行する
- Git/GitHub 操作は **`gh` CLI** を使用する（`git push --force` / `git reset --hard` 禁止）

Claude Code での詳細手順: `/github-workflow` スキル参照
